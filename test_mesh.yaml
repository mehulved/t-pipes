- type: mesh
  config:
    mapping:
      users:
        - type: csv_source
          config: { path: ./dummy.csv }
      todo:
        steps:
          - type: http_source
            config: { url: https://jsonplaceholder.typicode.com/todos/1 }
          - type: json_parser
- type: print
