# Project TODOs

- [x] **XML/HTML Support**: Added support for parsing XML and HTML content in `JsonParser` (or rename to `ContentParser`) and relevant blocks.
- [x] **Pipe Registry & Metadata**: 
    - [x] Store the original filepath of the pipeline configuration in the pickled data or a separate registry.
    - [x] Enable listing of available/designed pipelines.
    - [x] specific command to valid run a specific pipe from the registry.
- [x] **Data Export**: Support exporting pipeline data to JSON, XML, HTML, and CSV files.
- [x] **CSV Support**: Support CSV files as input sources.
- [ ] **Packaging**: Create a pip package.
