- type: concat
  config:
    sources:
      - steps:
          - type: file_source
            config:
              path: ./dummy.csv
          - type: csv_parser
      - steps:
         - type: http_source
           config:
             url: https://jsonplaceholder.typicode.com/todos/1
         - type: json_parser
- type: print
